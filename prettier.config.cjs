// @ts-check

/** @type{import('prettier').Config} */
const config = {
    semi: true,
    trailingComma: 'es5',
    tabWidth: 4,
    singleQuote: true,
    printWidth: 80,
};

module.exports = config;
